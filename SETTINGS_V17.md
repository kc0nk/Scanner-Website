# KCONK Suite v17 — Settings Window

- Gear icon opens a dedicated non-modal Settings window.
- Appearance section provides Dark/Light theme switching.
- Navigation order can be reset to the default eight modules.
- Settings window shows KCONK Suite version and release metadata.
- Main fixed-canvas dashboard/navbar remains unchanged.
