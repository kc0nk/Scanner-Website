# Proxy UI v22

Proxy now has a secondary navigation bar directly below the primary KCONK navbar:

- INTERCEPT
- HTTP HISTORY

The proxy workspace is currently a UI shell. Intercept has controls for the future intercept queue workflow; HTTP History has the request table and request/response inspector shell.

The main navbar and fixed canvas are unchanged.
