# KCONK Suite — Navbar v12

Fixed navbar separator:
- Explicit 1px line is rendered as a child widget across the full 1366px canvas.
- The brand area and all 8 navigation items share the same continuous baseline.
- The CSS border on the outer navbar was removed to prevent child widgets from visually covering it.
