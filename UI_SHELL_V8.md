# KCONK Suite UI Shell v8

- Navbar: Dashboard, Workflow, Proxy, Intruder, Repeater, Scanning, Collaborator, Meta Workflow NC.
- Main display is intentionally blank for the UI rebuild phase.
- Fixed canvas architecture is preserved.
- Existing functional source modules remain in the repository but are not mounted into the visible pages yet.
- Proxy is now a first-class primary navbar item.
